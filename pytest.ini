[pytest]
# Run only the focused test suites for the pkms prototype and tasks3
testpaths = tests tasks3/tests

# Ignore legacy or external prototype folders that cause unrelated test collection
norecursedirs = tasks4 tasks5 .git venv env __pycache__
